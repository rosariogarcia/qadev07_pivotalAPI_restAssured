Feature: Update a story in project from pivotal tracker

  Background: Create Project
    Given I send a POST request to /projects with:
      | name   | test_project_to9 |
      | public | true             |

    And I send a POST request to /stories with:
      | name     | new_Story |
      | estimate | 1         |

  Scenario: Edit a story
    Given I send a PUT request to /stories with:
      | name | name_set |
    Then I expect status code 200